# My Blog

## use jekyll

- [ ] rename post files and move asset

jekyll need ruby, install rvm first

```bash
# rvm install 3.0.0
# rvm use 3.0.0

# codespace has rvm
bundle install
```

usage:

```bash
source /etc/profile.d/rvm.sh # add to zshrc
# jekyll serve
bundle exec jekyll serve
```

jekyll admonition 写法 https://github.com/Helveg/jekyll-gfm-admonitions

## Post Style

https://jekyllrb.com/docs/posts/#creating-posts

Post md name should be `YEAR-MONTH-DAY-title.MARKUP`.
